import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gjpe.vehiculos',
  appName: 'vehiculos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
